---
phase: "01-foundation"
plan: "01-01"
type: "standard"
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - tailwind.config.ts
  - postcss.config.mjs
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - .gitignore
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
  - DS-03
must_haves:
  - "Next.js 14 App Router project exists at /Users/sunny/portfolio with src/ directory"
  - "`npm run dev` starts without errors and serves on localhost:3000"
  - "TypeScript compiles with no errors (`npx tsc --noEmit` passes)"
  - "Tailwind CSS is installed and PostCSS config is present"
  - "anime.js and framer-motion are in package.json dependencies"
  - "Bangers font loaded from Google Fonts in layout.tsx"
  - "JetBrains Mono font loaded from Google Fonts in layout.tsx"
  - "globals.css imports both font families and applies them to :root"
---

# Plan 01-01: Next.js Project Scaffold

## Objective

Bootstrap the Next.js 14 App Router project with TypeScript, install all required dependencies (Tailwind CSS, anime.js, Framer Motion), and configure Google Fonts (Bangers + JetBrains Mono). This is the foundation every subsequent plan builds on.

## Context

- Project directory: `/Users/sunny/portfolio`
- Tech stack locked: Next.js 14 App Router, TypeScript, Tailwind CSS, anime.js, Framer Motion
- Fonts: Bangers (display/manga headings) + JetBrains Mono (code/body)
- Commits must be signed: `git commit -s -m`
- Working on branch `phase/01-foundation` (create it)
- No existing Next.js project — directory is empty except for `.planning/` and `.git/`

## Tasks

<task type="auto">
  <name>Task 1: Create branch and scaffold Next.js 14 project</name>
  <files>
    package.json, tsconfig.json, next.config.ts, postcss.config.mjs, .gitignore,
    src/app/layout.tsx, src/app/page.tsx, src/app/globals.css
  </files>
  <action>
Create branch and scaffold the Next.js project:

```bash
cd /Users/sunny/portfolio
git checkout -b phase/01-foundation
```

Use `create-next-app` to scaffold inside the current directory. Since the directory already has `.git` and `.planning/`, use the `--skip-git` flag and scaffold in-place:

```bash
npx create-next-app@14 . \
  --typescript \
  --tailwind \
  --eslint \
  --app \
  --src-dir \
  --import-alias "@/*" \
  --skip-git \
  --yes
```

If `create-next-app` asks whether to overwrite existing files, confirm yes. The `.planning/` directory and `.git/` should not be touched.

After scaffolding, verify the structure exists:
- `src/app/layout.tsx`
- `src/app/page.tsx`
- `src/app/globals.css`
- `tailwind.config.ts`
- `next.config.ts`
- `package.json`

Do NOT modify any content yet — just verify the scaffold succeeded.
  </action>
  <verify>
    <automated>cd /Users/sunny/portfolio && ls src/app/layout.tsx src/app/page.tsx tailwind.config.ts next.config.ts package.json && node -e "const p = require('./package.json'); console.log('next:', p.dependencies.next)"</automated>
  </verify>
  <done>Next.js 14 project scaffolded with App Router, TypeScript, and Tailwind CSS. All scaffold files exist.</done>
</task>

<task type="auto">
  <name>Task 2: Install animation libraries and configure Google Fonts</name>
  <files>
    package.json, src/app/layout.tsx, src/app/globals.css
  </files>
  <action>
Install animation dependencies:

```bash
cd /Users/sunny/portfolio
npm install animejs framer-motion
npm install --save-dev @types/animejs
```

Note: The package is `animejs` (not `anime.js`). Current stable version is 3.x. Import syntax: `import anime from 'animejs'`.

Update `src/app/layout.tsx` to load Google Fonts. Replace the existing font imports with Bangers and JetBrains Mono:

```tsx
import type { Metadata } from 'next'
import { Bangers, JetBrains_Mono } from 'next/font/google'
import './globals.css'

const bangers = Bangers({
  weight: '400',
  subsets: ['latin'],
  variable: '--font-bangers',
  display: 'swap',
})

const jetbrainsMono = JetBrains_Mono({
  subsets: ['latin'],
  variable: '--font-jetbrains-mono',
  display: 'swap',
})

export const metadata: Metadata = {
  title: 'Shreyansh Sancheti',
  description: 'Software Engineer — Distributed Systems, Infrastructure, Open Source',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className={`${bangers.variable} ${jetbrainsMono.variable}`}>
      <body className={jetbrainsMono.className}>{children}</body>
    </html>
  )
}
```

Update `src/app/globals.css` — keep Tailwind directives, add font variable declarations:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

:root {
  --font-manga: var(--font-bangers), 'Bangers', cursive;
  --font-mono: var(--font-jetbrains-mono), 'JetBrains Mono', 'Courier New', monospace;
}
```

Do NOT add color tokens yet — those belong in Plan 01-02.
  </action>
  <verify>
    <automated>cd /Users/sunny/portfolio && node -e "const p = require('./package.json'); const hasFM = 'framer-motion' in p.dependencies; const hasAnime = 'animejs' in p.dependencies; console.log('framer-motion:', hasFM, 'animejs:', hasAnime); if (!hasFM || !hasAnime) process.exit(1)"</automated>
  </verify>
  <done>animejs and framer-motion installed. layout.tsx loads Bangers and JetBrains Mono from Google Fonts with CSS variable names `--font-bangers` and `--font-jetbrains-mono`. globals.css maps them to `--font-manga` and `--font-mono`.</done>
</task>

<task type="auto">
  <name>Task 3: Verify dev server and commit scaffold</name>
  <files>
    src/app/page.tsx
  </files>
  <action>
Replace the default Next.js boilerplate in `src/app/page.tsx` with a minimal placeholder that confirms the design system will load:

```tsx
export default function Home() {
  return (
    <main className="min-h-screen bg-black flex items-center justify-center">
      <p
        className="text-white"
        style={{ fontFamily: 'var(--font-mono)' }}
      >
        portfolio loading...
      </p>
    </main>
  )
}
```

Start the dev server briefly to verify it starts without errors, then stop it:

```bash
cd /Users/sunny/portfolio
timeout 15 npm run dev &
sleep 10
curl -s -o /dev/null -w "%{http_code}" http://localhost:3000 | grep -q "200" && echo "Dev server OK" || echo "Dev server failed"
pkill -f "next dev" || true
```

Run TypeScript check:

```bash
cd /Users/sunny/portfolio && npx tsc --noEmit
```

Then commit everything on the `phase/01-foundation` branch:

```bash
cd /Users/sunny/portfolio
git add -A
git commit -s -m "feat(infra): scaffold Next.js 14 App Router with TypeScript and Tailwind

- Next.js 14 with App Router and src/ directory structure
- TypeScript configured with strict mode
- Tailwind CSS with PostCSS
- animejs and framer-motion installed
- Bangers + JetBrains Mono loaded via next/font/google
- Font CSS variables: --font-manga, --font-mono

Requirements: INFRA-01, INFRA-02, INFRA-03, DS-03"
```
  </action>
  <verify>
    <automated>cd /Users/sunny/portfolio && git log --oneline -1 | grep -q "scaffold" && echo "Commit OK" && npx tsc --noEmit && echo "TypeScript OK"</automated>
  </verify>
  <done>Dev server starts and returns 200. TypeScript compiles clean. Scaffold committed to `phase/01-foundation` branch with signed commit.</done>
</task>

## Success Criteria

- `git checkout phase/01-foundation` succeeds
- `npm run dev` serves page on localhost:3000 returning HTTP 200
- `npx tsc --noEmit` exits 0 with no errors
- `package.json` dependencies include `next@14.x`, `animejs`, `framer-motion`
- `src/app/layout.tsx` imports Bangers and JetBrains_Mono from `next/font/google`
- `src/app/globals.css` defines `--font-manga` and `--font-mono` CSS variables

## Output

Produces:
- Complete Next.js 14 App Router project structure under `/Users/sunny/portfolio`
- Branch: `phase/01-foundation`
- Installed: animejs, framer-motion, TypeScript types
- Fonts: Bangers (`--font-manga`) and JetBrains Mono (`--font-mono`) configured

Consumed by:
- Plan 01-02 (design tokens — extends tailwind.config.ts and globals.css)
- Plan 01-03 (visual components — creates component files)
